{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "groupName": "aws",
      "matchPackagePrefixes": ["^@aws-sdk/", "^aws$"],
      "matchUpdateTypes": ["minor", "patch"],
      "minimumReleaseAge": "3 days"
    },
    {
      "groupName": "aws",
      "matchPackagePrefixes": ["^@aws-sdk/", "^aws$"],
      "matchUpdateTypes": ["major"]
    }
  ]
}
